
-- gh:karnull/switchboard.nvim

return {
    cd_root =  true,

    commands = {
        run    = 'devcontainer exec uv run main',
        build  = 'devcontainer exec uv run pyinstaller --onefile --name bin --specpath ./build --console --noconfirm ./*/__main__.py',
        test   = 'devcontainer exec uv run test',
    },

    binds = {
        label = 'O#<Esc>79A=<Esc>jgUU',
        import= 'Iimport ',
    }
}

